import { TestBed, inject } from '@angular/core/testing';
import { NotificationsService } from 'angular2-notifications';

import { NotificationService } from './notification.service';

// describe('NotificationService', () => {
//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       providers: [
//         NotificationService,
//         NotificationsService
//       ]
//     });
//   });

//   it('#should create notification by createNotification function', inject([NotificationService], (service: NotificationService) => {
//     spyOn((service as any)._notificationsService, 'success');
//     service.createNotification('success', 'success', 'success');
//     expect((service as any)._notificationsService.success).toHaveBeenCalled();
//   }));
// });
/**
 * wrote in 2018/3/30
 * */
